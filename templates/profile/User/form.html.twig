{% extends 'profile/base.html.twig' %}

{% block body %}

    <h1>{{ title }}</h1>
    <div class="container">
        <div class="row no-gutters justify-content-center">
            <div class="col-5">
                <div class="mt-3">
                    {{ form(form) }}
                </div>
            </div>
        </div>
    </div>

{% endblock %}